configuration:
  log4j:
    path:
environment:
  prod:
    local:
      port:
    remote:
      ssh:
        host:
        port:
        key:
      mysql:
        host:
        port:
        id:
        pwd:
  stg:
    local:
      port:
    remote:
      ssh:
        host:
        port:
        key:
      command:
        line:  
      
    